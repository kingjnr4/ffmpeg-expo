# Changesets

Add a Changeset for changes that affect the published `ffmpeg-expo` package.

Changes to private tooling, native builders, or the example app only need a Changeset when they alter the published package output or runtime behaviour.
